{
    "private": true,
    "name": "@screener/db",
    "version": "1.0.0",
    "description": "Screener databasse",
    "scripts": {
        "build": "prisma generate && tsup ./index.ts",
        "reset": "prisma migrate reset",
        "migrate": "prisma migrate dev",
        "deploy": "prisma migrate deploy",
        "studio": "prisma studio"
    },
    "main": "index.ts",
    "dependencies": {
        "@prisma/client": "4.16.1",
        "commander": "^8.3.0",
        "got": "^11.8.3",
        "prisma": "^4.16.1"
    },
    "engines": {
        "node": ">=14.0.0"
    },
    "devDependencies": {
        "tsup": "^7.0.0"
    }
}